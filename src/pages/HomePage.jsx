import React from "react";
import { Typography } from "@mui/material";

const HomePage = () => {
  return <Typography variant="h4">Bienvenido a la página de inicio</Typography>;
};

export default HomePage;